global:
    scrape_interval: 15s

scrape_configs:
    - job_name: "prometheus"

      static_configs:
          - targets:
                - localhost:9090

# Ajout apres installation de Node Exporter.
    - job_name: "node-exporter"

      static_configs:
          - targets:
                - host.docker.internal:9100